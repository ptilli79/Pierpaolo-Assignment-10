package com.projects.cavany.domain;

public enum RoleUser {
    ROLE_USER, // A regular user with basic permissions
    ROLE_ADMIN; // An admin with elevated permissions

    // Additional roles can be added as needed.
}
